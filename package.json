{
  "name": "readme",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --minify --target bun src/index.ts --outfile dist/index.js",
    "start": "bun dist/index.js",
    "docker:up": "docker compose up -d --build"
  },
  "dependencies": {
    "hono": "^4.12.18",
    "html-entities": "^2.6.0",
    "ky": "^2.0.2",
    "remeda": "^2.34.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^25.6.2"
  }
}
